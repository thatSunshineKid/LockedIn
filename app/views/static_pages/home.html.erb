<!DOCTYPE html>
<html>
<center>
<% if logged_in? %>
  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <%= render 'shared/user_info' %>
      </section>
      <section class="micropost_form">
        <%= render 'shared/micropost_form' %>
      </section>
    </aside>
    <div class="col-md-8">
      <h3>Noted Feed</h3>
      <%= render 'shared/feed' %>
    </div>
  </div>
<% else %>
  <div class="center jumbotron">
  <h1>What important thing are you forgetting right now?</h1>

  <h2>
    Noted is an easy to use application to assist you in remembering those pesky day-to-day things that have a habit of slipping your mind!
  </h2>

  <%= link_to "Sign up now!", signup_path, class: "btn btn-lg btn-primary" %>
</div>

<%= link_to image_tag("forget.png", alt: "Rails logo"),
            '/' %>
<% end %>
        </center>
</html>
